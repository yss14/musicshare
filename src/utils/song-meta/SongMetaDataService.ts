export class SongMetaDataService {
	private static readonly songFormats: ISongMetaDataFormat[] = [

	];

	public static analyse(originalFilename: string, extension: string, audioBuffer: Buffer) {

	}
}