{
	"name": "@musicshare/frontend",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"start": "cross-env CI=true react-app-rewired start",
		"dev": "npm run start",
		"build": "react-app-rewired build",
		"test": "react-app-rewired test",
		"eject": "react-scripts eject",
		"verify": "tsc --incremental false --composite false --noEmit --noUnusedLocals --skipLibCheck",
		"lint": "eslint . --ext .js,.ts,.tsx,.jsx"
	},
	"eslintConfig": {
		"extends": "react-app"
	},
	"browserslist": [
		">0.2%",
		"not dead",
		"not ie <= 11",
		"not op_mini all"
	],
	"dependencies": {
		"@musicshare/shared-types": "*",
		"@musicshare/react-graphql-client": "*"
	},
	"devDependencies": {}
}